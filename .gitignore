__pycache__/
*.py[cod]
*.log
*.out
*.err
*.tmp
*.ipynb_checkpoints/
*.ipynb
*.csv
*.parquet
.ruff_cache/
.pytest_cache/
.mypy_cache/
.idea/
# artifacts (not source) — LEADING SLASH anchors each to the repo root, so
# e.g. /validation/ ignores only the top-level output dir, NOT the tracked
# code under src/globalmacro/validation/. (Unanchored `validation/` would match
# at any depth and silently re-ignore the code.)
/data/
/datasets/
/characteristics/
/validation/
/analysis/
/docs/
/figures/
# validation output artifacts co-located with tracked validation .py code
# (build repoints VALIDATION_OUTPUT to the ignored /validation/ later; these
#  stale/interim outputs live in the code dir — ignore them, keep the code tracked)
src/globalmacro/validation/*.pdf
src/globalmacro/validation/*.txt
src/globalmacro/validation/datastream_comparison/
src/analysis/validation/
# confidential external validation plugin — never commit
src/globalmacro/validation/external_comparison.py
# reads the confidential external reference panel via external_comparison.py's
# machinery -- same reasoning, never commit
src/globalmacro/validation/provenance_gate.py
tests/test_provenance_gate.py
# Local validation/verification tooling — the owner's harnesses (gates, reruns,
# spike/coverage/integrity checks, cross-contract and roll-boundary measures) that
# confirm fixes on our end but are NOT part of building the dataset. Deliberately
# local-only, never shipped to other researchers; the pipeline never imports any of
# it. The scripts and their tests live untracked here.
scripts/misc/
# The Tier-2 onboarding gate's src module + its test stay ignored in place (they
# are not scripts): the owner's local harness for accepting a new Tier 2 asset
# (spikes, gaps, coverage, splice integrity). Contact the owner to add an asset.
src/globalmacro/validation/asset_quality.py
tests/test_asset_quality.py
.DS_Store
.env
.claude/
.superpowers/
assetlist.pdf
AGENTS.md
CLAUDE.md
/slurm/logs/
/reference/
/papers/
old-data/
# methodology docs — intentionally untracked here, not an oversight. Their
# history lives on the local productionize / docs branches. Ignoring a path
# does NOT stop tracked files arriving via a merge, so merge those branches
# into master only with --squash, never plain.
/documentation/
